const fs = require("fs/promises");
const fileName1 = "myfile1.txt";
const fileName2 = "myfile2.txt";

const reader = async (fileName1, fileName2) => {
    //Write your code here
    //Don't change function name

   // read the content of myfile1 and myfile2
  
  // merge the content of the two files
  
};

module.exports = reader;

